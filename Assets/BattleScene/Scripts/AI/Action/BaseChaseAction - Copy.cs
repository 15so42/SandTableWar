
    using BehaviorDesigner.Runtime.Tasks;
    using UnityEngine.AI;

    public class ChaseAction : Action
    {
        public NavMeshAgent navMeshAgent;
       // navMeshAgent.SetDestination(controller.chaseTarget.transform.position);
        
    }
